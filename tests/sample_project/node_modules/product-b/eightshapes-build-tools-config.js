'use strict';

module.exports = {
    codeNamespace: "product-b",
    manageNunjucksEnv: function(env) {
        env.addFilter('stomp', function(obj) {
          return 'STOMP!';
        });
    }
};
